// Full widget smoke tests require mocking of AppDatabase, biometric service,
// and GoRouter. Those concerns are covered by feature-level unit tests.
// This file is retained as a placeholder to satisfy the flutter test runner.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — full integration test pending Sprint 2', (
    WidgetTester tester,
  ) async {
    expect(1, 1);
  });
}
